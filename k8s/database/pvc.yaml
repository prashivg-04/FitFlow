apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: postgres-pvc
  namespace: fitflow
  
spec:
  resources:
    requests:
      storage: 5Gi
      
  accessModes:
    - ReadWriteOnce
  
  storageClassName: manual
